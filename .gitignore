# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtualenv
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Database files (if specific like SQLite, or general patterns)
*.db
*.sqlite
*.sqlite3
# database/*.json # Example: ignore all json files in database dir if they are products, not sources
                    # However, for this project, ideas.json IS the database, so it might be versioned initially
                    # or backed up elsewhere. For now, let's not ignore it to see output.
                    # Let's ignore specific test databases generated by modules.
database/*_test_ideas.json
database/*_test_ideas_db.json
database/ga_module_test_ideas.json
database/eval_module_test_ideas.json
database/main_demo_ideas.json

# IDE / Editor specific
.vscode/
.idea/
*.swp
*.swo
*~

# OS specific
.DS_Store
Thumbs.db
